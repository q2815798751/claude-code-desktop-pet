# runtime artifacts
term.pid
_server.log
*.log
# persisted UI prefs (theme / font scale), window position, and installer-resolved
# runtime paths (node/claude) - machine-specific runtime user state
app/config.json
app/host.json
app/runtime.ini
# token usage ledger - a running total over this machine's transcripts, rebuilt
# from scratch if deleted, so it must never be committed or shipped
app/usage.json
app/usage.json.tmp
edge_profile/
host/webview2data/
host/ClaudePet.Host.exe
host/*.dll
node_modules/
dist/

# os noise
.DS_Store
Thumbs.db
desktop.ini
/ClaudePet-Setup.exe
